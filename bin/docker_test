#!/usr/bin/env bash
docker run --rm -v `pwd`/lib:/app/lib -v `pwd`/lib/callback:/app/lib/callback -v `pwd`/bin:/app/bin -v `pwd`/spec:/app/spec -v `pwd`/src:/app/src mosop/run:latest bin/build_and_test
